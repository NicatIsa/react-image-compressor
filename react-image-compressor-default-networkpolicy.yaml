apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: react-image-compressor-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/react-image-compressor-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/react-image-compressor-default: "true"
